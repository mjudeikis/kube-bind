---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: collections.catalog.kbind.io
spec:
  group: catalog.kbind.io
  names:
    categories:
    - kbind
    kind: Collection
    listKind: CollectionList
    plural: collections
    singular: collection
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.title
      name: Title
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Collection groups Exports for UI/CLI browsing. Pure presentation; nothing
          binds to a Collection.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CollectionSpec is a titled grouping of Exports.
            properties:
              description:
                description: description explains the grouping.
                type: string
              exports:
                description: exports lists the member Exports by name.
                items:
                  description: ExportRef references an Export by name.
                  properties:
                    name:
                      description: name of the Export.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              title:
                description: title is the human-facing name of the group.
                minLength: 1
                type: string
            required:
            - exports
            - title
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
