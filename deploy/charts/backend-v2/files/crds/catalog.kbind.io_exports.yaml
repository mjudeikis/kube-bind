---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: exports.catalog.kbind.io
spec:
  group: catalog.kbind.io
  names:
    categories:
    - kbind
    kind: Export
    listKind: ExportList
    plural: exports
    singular: export
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.title
      name: Title
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Export is one curated offering in the provider's catalog: human-facing
          metadata plus defaults on top of one or more exported APIs. The catalog is
          derived-from-core-truth: an Export listing an API that is not actually
          exported (label/boundary) gets a condition and is hidden by the gateway —
          the export label remains the source of truth, the catalog is presentation
          and defaults. Lives on the provider; the konnector never sees it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExportSpec describes one offering.
            properties:
              apis:
                description: |-
                  apis lists the exported APIs a binding to this offering syncs, by CRD
                  name on the provider ("<plural>.<group>").
                items:
                  description: |-
                    APIRef identifies an exported API by its CRD name on the provider,
                    i.e. "<plural>.<group>" (for example "mangodbs.mangodb.io"). Under the
                    OpenAPI schema source there is no CRD object behind the name; it is still
                    just resource + group.
                  properties:
                    name:
                      description: name is the CRD name of the exported API, "<plural>.<group>".
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              defaults:
                description: |-
                  defaults are copied into the generated ClusterBinding of the one-apply
                  bundle.
                properties:
                  conflictPolicy:
                    description: |-
                      conflictPolicy is the default conflict behavior for the generated
                      ClusterBinding.
                    enum:
                    - Fail
                    - Adopt
                    type: string
                  relatedResources:
                    description: |-
                      relatedResources are the related-resource selectors for the generated
                      ClusterBinding.
                    items:
                      description: |-
                        RelatedResource selects auxiliary objects (Secrets/ConfigMaps) to sync
                        alongside the bound instances.
                      properties:
                        direction:
                          description: direction the related resource flows.
                          enum:
                          - FromProvider
                          - FromConsumer
                          type: string
                        group:
                          default: ""
                          description: group of the related resource. Empty string
                            for the core group.
                          type: string
                        resource:
                          description: |-
                            resource is the plural resource name. Only "secrets" and "configmaps"
                            are permitted in core.
                          enum:
                          - secrets
                          - configmaps
                          type: string
                        selector:
                          description: |-
                            selector restricts which objects are synced. Only labelSelector and
                            named selectors are supported in core (no JSONPath reference-following).
                          properties:
                            labelSelector:
                              description: labelSelector selects related objects by
                                label.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            names:
                              description: names selects related objects by exact
                                name.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                          type: object
                      required:
                      - direction
                      - group
                      - resource
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              description:
                description: description explains the offering to a human choosing
                  it.
                type: string
              docs:
                description: docs optionally links to the offering's documentation.
                type: string
              icon:
                description: icon optionally points at an icon for UIs.
                properties:
                  url:
                    description: url of the icon image.
                    minLength: 1
                    type: string
                required:
                - url
                type: object
              title:
                description: title is the human-facing name of the offering.
                minLength: 1
                type: string
            required:
            - apis
            - title
            type: object
          status:
            description: ExportStatus is the observed state of an Export.
            properties:
              conditions:
                description: 'conditions: Ready (all listed APIs are actually exported).'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
